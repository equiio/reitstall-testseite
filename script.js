const menuButton=document.querySelector('.menu-button');const nav=document.querySelector('#main-nav');menuButton?.addEventListener('click',()=>{const open=menuButton.getAttribute('aria-expanded')==='true';menuButton.setAttribute('aria-expanded',String(!open));nav.classList.toggle('open',!open)});nav?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{nav.classList.remove('open');menuButton?.setAttribute('aria-expanded','false')}));
const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');observer.unobserve(entry.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));
const time=document.querySelector('#live-time');if(time){const update=()=>{time.textContent='Demo-Stand · '+new Intl.DateTimeFormat('de-DE',{weekday:'long',hour:'2-digit',minute:'2-digit'}).format(new Date())+' Uhr'};update();setInterval(update,60000)}
const pasture=document.querySelector('#pasture-demo');const count=document.querySelector('#pasture-count');let pastureState=0;pasture?.addEventListener('click',()=>{pastureState=(pastureState+1)%3;count.textContent=['12','8','4'][pastureState];pasture.textContent=['Status simulieren','Nächste Gruppe','Zurücksetzen'][pastureState]});
const dialog=document.querySelector('#booking-dialog');document.querySelectorAll('.open-booking').forEach(button=>button.addEventListener('click',()=>dialog.showModal()));document.querySelector('.dialog-close')?.addEventListener('click',()=>dialog.close());dialog?.addEventListener('click',event=>{if(event.target===dialog)dialog.close()});
const form=document.querySelector('#booking-form');form?.addEventListener('submit',event=>{event.preventDefault();form.hidden=true;document.querySelector('#booking-success').hidden=false});
const toast=document.querySelector('#toast');function showToast(message){toast.textContent=message;toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),3500)}document.querySelector('.toast-trigger')?.addEventListener('click',()=>showToast('Demo-Nachricht geöffnet — keine echten Mitteilungen vorhanden.'));document.querySelector('#privacy-info')?.addEventListener('click',()=>showToast('Privat: keine Cookies, kein Tracking, keine Übertragung und kein lokaler Speicher.'));
